# Nextcloud All-in-one

## Installation :- 

```bash
docker compose up -d --build --remove-orphans
```

Need to use NPM (Nginx proxy manager) / caddy / traefik for reverse proxy. 

Reference :- https://github.com/TechHutTV/homelab/blob/main/cloud/compose.yaml