http:
  routers:
    traefik-dashboard-router:
      entryPoints:
        - websecure
      rule: Host(`traefik.homelab.local`) || Host(`alpine-traefik.tailcda03.ts.net`)
      service: api@internal
      tls: {}
      middlewares:
        - traefik-strip-prefix
        - redirect-https
