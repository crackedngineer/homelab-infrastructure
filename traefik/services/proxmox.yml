http:
  routers:
    proxmox-router:
      entryPoints:
        - web
      rule: Host(`proxmox.homelab`)
      service: proxmox-svc

  services:
    proxmox-svc:
      loadBalancer:
        serversTransport: "pve"
        servers:
          - url: "https://192.168.31.249:8006"

  serversTransports:
    pve:
      insecureSkipVerify: true
