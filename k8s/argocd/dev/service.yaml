apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  annotations:
    tailscale.com/expose: "true"
spec:
  selector:
    app: myapp
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
